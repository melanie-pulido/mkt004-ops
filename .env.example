# SFDX auth URLs — stored as GitHub Secrets (never commit real values)
# Generate with: sf org auth show-sfdx-auth-url --target-org <alias>
SF_AUTH_URL_ORG1=force://PlatformCLI::refreshToken@instance.salesforce.com
SF_AUTH_URL_ORG2=force://PlatformCLI::refreshToken@instance.salesforce.com
SF_AUTH_URL_ORG3=force://PlatformCLI::refreshToken@instance.salesforce.com

# Google Form Apps Script
# GITHUB_TOKEN stored as Script Property in Google Apps Script editor (not here)
